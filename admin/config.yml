backend:
  name: git-gateway
  branch: gh-pages # optional, default value is "master"
media_folder: 'assets/uploads'
collections:
  - name: 'blog'
    label: 'Blog'
    folder: '_posts/'
    fields:
      - { name: Title }
